# quartet-match
Create scratch quartets based on user-input ranges

## Setup

Point the server at a database; a simple SQLite file will do. Anything else may need additional drivers installed to the backend. See `server/config.py` for details.
